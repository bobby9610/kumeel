# Local administration only. Never prefix these secrets with VITE_.
supabase=
GITHUB_TOKEN=
# Public project URL and publishable/anon key live in src/supabase-config.json.
# They identify the project; access is enforced by database RLS.
